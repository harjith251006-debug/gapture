org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Deployment URL the WebView loads. Override in local.properties (gitignored)
# with `gapture.appUrl=https://your-deployment` or pass -Pgapture.appUrl=...
# Leave blank to show the in-app configuration screen.
gapture.appUrl=
